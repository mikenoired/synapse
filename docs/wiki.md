# Wiki

## Производительность сервера

После изменения серверного кода, которое может повлиять на скорость обработки HTTP-запросов, запускайте:

```bash
bun run smoke:server
```

Команда последовательно выполняет 5 сценариев: создание аккаунта, создание поста, его редактирование, загрузку случайной картинки из `test/assets`, удаление поста, удаление картинки и удаление аккаунта. Каждый сценарий использует новый аккаунт и в `finally` повторяет удаление аккаунта, если шаг завершился ошибкой.

Результат сохраняется в `docs/performance/server-smoke.json`: для каждой даты и шага фиксируются `avgMs`, `medianMs`, `p95Ms`, `p99Ms`, `minMs`, `maxMs`, `stdDevMs`, `avgReqPerSec`, `medianReqPerSec`, `successCount`, `errorCount` и последний commit (SHA и subject). Дополнительно сохраняются process CPU/RAM-снимки запуска в поле `system`. После успешного запуска в БД и MinIO не должно оставаться тестовых данных; единственный сохраняемый артефакт — статистика. Файл нужно коммитить вместе с изменением, чтобы видеть динамику.

Локальный интерактивный график доступен по адресу [http://localhost:3000/performance](http://localhost:3000/performance) после production-сборки и запуска. В dev-режиме его можно открыть как [http://localhost:5173/performance.html](http://localhost:5173/performance.html). Страница получает историю через `/api/performance` и позволяет выбрать шаг и метрику.

Smoke-тест требует запущенных PostgreSQL, Redis и MinIO, а также применённой схемы БД. К изменениям, требующим запуска smoke-тестов, относятся изменения в `apps/web/src/server`, серверных API-контрактах, middleware, базе данных, Redis/MinIO-интеграциях и конфигурации сборки сервера. Изменения только в UI обычно не требуют этого запуска.

## Размер серверного бандла

Чтобы собрать минифицированный серверный entrypoint и вывести его размер, запускайте:

```bash
bun run build:server:size
```

Результат сборки создаётся во временной, игнорируемой директории `apps/web/.server-build`.
