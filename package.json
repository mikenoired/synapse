{
	"name": "synapse",
	"version": "0.1.0",
	"private": true,
	"packageManager": "bun@1.3.14",
	"workspaces": [
		"packages/*",
		"apps/*"
	],
	"scripts": {
		"build:check": "bun run check && bun --filter @synapse/web build && bun run bundle:size",
		"build:server:size": "bun build apps/web/src/server/index.ts --outfile apps/web/.server-build/server.js --target bun --minify && bun scripts/bundle-size.ts --server",
		"bundle:size": "bun scripts/bundle-size.ts",
		"check": "bun run lint && bun run format:check && bun run typecheck && bun run test",
		"format": "bun x oxfmt --config .oxfmtrc.json --write .",
		"format:check": "bun x oxfmt --config .oxfmtrc.json --check .",
		"lint": "bun run lint:js",
		"lint:fix": "bun run lint:js:fix",
		"lint:js": "bun x oxlint . --config .oxlintrc.json --deny-warnings",
		"lint:js:fix": "bun x oxlint . --config .oxlintrc.json --fix",
		"test": "bun --filter @synapse/web test",
		"smoke:server": "JWT_SECRET=smoke-test-secret JWT_REFRESH_SECRET=smoke-test-refresh-secret API_RATE_LIMIT_MUTATION=1000 bun scripts/server-smoke.ts",
		"typecheck": "bun x tsc -p apps/web/tsconfig.json --noEmit && bun x tsc -p packages/ui/tsconfig.json --noEmit"
	},
	"devDependencies": {
		"@types/bun": "^1.3.14",
		"oxfmt": "^0.61.0",
		"oxlint": "^1.76.0",
		"typescript": "^7.0.2"
	},
	"overrides": {
		"prosemirror-model": "1.25.9",
		"prosemirror-state": "1.4.4",
		"prosemirror-transform": "1.12.0",
		"prosemirror-view": "1.41.9"
	}
}
